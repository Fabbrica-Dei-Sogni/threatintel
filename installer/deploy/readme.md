Questi script sono copiati dal make-release sulla folder deployments per la release versionata che sta creando. Gli script vengono poi eseguiti durante le fasi in cui si sceglie di registrare deregistrare il servizio systemd.

Le release generate sulla cartella deployments , per policy, non vengono mai cancellate automaticamente, per evitare perdite accidentali.

